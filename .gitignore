logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Supabase migration staging: exported rows + downloaded audio.
# Never commit these — see .kiro/skills/supabase-to-postgres/references/data-migration.md
export/

# Local secret files, read by getSecret() when IS_BLACK_ENV=true. Never committed.
# Run `npm run secrets:example` to create them from backend/local_secrets.example/,
# which IS committed and documents the file names and shapes each environment needs.
local_secrets/
